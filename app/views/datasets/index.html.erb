<h1>Fantasy Football Lineup Generator</h1>
<hr>
<p>A small rails app that takes fantasy football data and returns lineup permutations with the highest scoring potential.</p>

<p>Source available on github: <%= link_to "github.com/gxespino/daily_fantasy_sports", "https://github.com/gxespino/daily_fantasy_sports", target: "_blank" %>

<table class="table">
  <thead>
    <tr>
      <th colspan="3">Datasets</th>
    </tr>
  </thead>

  <tbody>
    <% @datasets.each do |dataset| %>
      <tr>
        <td><%= dataset.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<!-- 
%h1 Power Scraper

%p 
  A small rails app performing a live authenticated scrape of FirstEnergy power provider. Uses the ruby gems Mechanize & Nokogiri and grabs an accounts power usage history. 

%p
  Source available on github:
  %a{href: 'https://github.com/justindoody/power-scraper'} github.com/justindoody/power-scraper

%p
  Account Name:
  = @name

%table.table.table-hover
  %thead
    %tr
      %th Date
      %th KWH
      %th Cost
  %tbody
    - @usage.each do |key, val|
      %tr
        %td.table-text.col-sm-4= key
        %td.table-text.col-sm-4= val[0]
        %td.table-text.col-sm-4= val[1]


= column_chart @data, colors: ['#2398ce'], discrete: true
--> 
